      <div class="jumbotron">
        <div class="container"><h1>un event !</h1>
         </div>
        </div>

      <div class="container">
        <div class="row">

            <div class="col-md-4">
              <h2><%= @event.title %></h2>
              <p><%= @event.description %></p>
              <p><a class="btn btn-secondary" href="<%= event_path(@event) %>" role="button">View details &raquo;</a></p>
            </div>
        </div>
        </div>
